{
  "name": "helios-cli",
  "version": "0.10.0",
  "description": "Agent-friendly CLI for running and controlling Helios Web sessions.",
  "private": false,
  "type": "module",
  "bin": {
    "helios": "./bin/helios.js"
  },
  "scripts": {
    "build-client": "vite build",
    "build": "npm run build-client",
    "test": "npm run build-client && node --test tests/*.test.js"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:filipinascimento/helios-cli.git"
  },
  "license": "MIT",
  "files": [
    "bin",
    "dist",
    "skills",
    "src",
    "README.md",
    "vite.config.js"
  ],
  "dependencies": {
    "helios-network": "^0.10.0",
    "playwright": "^1.56.1",
    "ws": "^8.18.0",
    "helios-web": "^0.10.0"
  },
  "devDependencies": {
    "vite": "^5.4.10"
  }
}
